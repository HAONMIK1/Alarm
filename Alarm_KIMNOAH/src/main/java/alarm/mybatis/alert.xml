<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="alarm.AlertDTO">

	  <insert id="insertAlert" parameterType="alarm.model.AlertDTO">
  	insert into AlertTable values(#{location},#{grade},#{time},#{alert_check})
  </insert>
  
</mapper>